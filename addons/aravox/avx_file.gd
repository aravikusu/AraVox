## Basically this just exists so that .avx files can be imported and tracked
## in the FileSystem tray. It has no other use, so no point in using it for anything.
extends Resource

@export var source_path: String = ""
